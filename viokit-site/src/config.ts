/**
 * Where the interest form should POST submissions.
 *
 * Leave empty to run in demo mode (submissions are simulated, nothing is sent
 * anywhere). Point this at any endpoint that accepts JSON — for example a
 * Formspree form: `https://formspree.io/f/<your-form-id>`.
 */
export const INTEREST_FORM_ENDPOINT = "";
