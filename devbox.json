{
  "packages": ["bun@1.3.13", "nodejs@22.23.2"],
  "shell": {
    "init_hook": ["export PATH=\"$PWD/node_modules/.bin:$PATH\""],
    "scripts": {
      "setup": ["bun install"],
      "test": ["bun run test"],
      "typecheck": ["bunx tsc --noEmit"],
      "check": ["npm exec -- ultracite check"]
    }
  }
}
